Give me Top 20 python problem solving questions, that are asked by pakistani companies to solve for a dev with 3 y exp, give only problem statments not sol

Given a list of integers, find the first non-repeating element.

Write a function to check whether two strings are anagrams of each other.

Given a string, find the longest substring without repeating characters.

Implement a function to reverse words in a sentence without using built-in reverse methods.

Find the second largest number in a list without sorting the list.

Given a list of numbers, move all zeros to the end while maintaining the order of non-zero elements.

Check whether a given string is a palindrome while ignoring spaces and special characters.

Given two sorted arrays, merge them into one sorted array without using built-in sort functions.

Find duplicate elements in a list and return their frequency count.

Implement a simple rate limiter that allows only N requests per user within a time window.

Given a nested dictionary, flatten it into a single-level dictionary with dot-separated keys.

Write a function to group a list of words by their anagrams.

Design an API log parser that returns the top 3 most frequently accessed endpoints.

Given a matrix, print its elements in spiral order.

Implement LRU (Least Recently Used) Cache using Python classes.

Write a function to validate whether parentheses/brackets in a string are balanced.

Given employee check-in/check-out logs, calculate total working hours for each employee.

Build a function that detects cyclic references in a directed graph.

Given transaction records, find suspicious duplicate transactions occurring within a short time window.

Create a task scheduler where tasks have dependencies and must execute in the correct order.